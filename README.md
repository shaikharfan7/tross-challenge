# LinkedIn Profile API

A reverse-engineered API for retrieving structured LinkedIn profile data.

Built as part of an engineering challenge focused on reverse engineering HTTP APIs, data extraction, API design, and deployment.

## Overview

The API accepts a LinkedIn profile URL and returns the profile information available through the underlying LinkedIn endpoints as structured JSON.

The initial implementation focuses on the fields required by the challenge:

* Name
* Headline
* Location
* About
* Experience
* Education
* Skills
* Certifications
* Languages
* Profile images

Additional fields may be exposed as the underlying responses are explored and verified.

The implementation communicates directly with LinkedIn over HTTP. It does **not** use browser automation, Playwright, Puppeteer, Selenium, or a headless browser.

## Example

### Request

```http
POST /api/v1/profile
Content-Type: application/json
```

```json
{
  "url": "https://www.linkedin.com/in/example/"
}
```

### Response

The response normalizes LinkedIn data into a structured schema. Note that empty arrays (Skills, Education, Certifications, Languages) are present but empty due to the technical limitations documented in the Reverse Engineering section—specifically, LinkedIn's dynamic session validation and lazy-loaded profile sections.

```json
{
  "profile": {
    "url": "https://www.linkedin.com/in/shaikharfan7/",
    "name": "Arfan Shaikh",
    "headline": "5+ years building products for founders across Europe.",
    "location": "Ponda, Goa, India",
    "about": null,
    "image": "https://media.licdn.com/...",
    "backgroundImage": "https://media.licdn.com/...",
    "experience": [
      {
        "title": "Software Engineer / Expo Developer",
        "company": "NEOSTARTER GmbH",
        "location": "Düsseldorf, North Rhine-Westphalia, Germany",
        "description": "Software Engineer building a gamified environmental cleanup app...",
        "startDate": "Jun 2025",
        "endDate": "Present",
        "image": null
      }
    ],
    "education": [],
    "skills": [],
    "certifications": [],
    "languages": []
  }
}
```

#### Making API Calls

To retrieve a LinkedIn profile, send a `POST` request to `/api/v1/profile` with a LinkedIn profile URL:

```bash
curl -X POST http://localhost:3000/api/v1/profile \
  -H 'Content-Type: application/json' \
  -d '{"url": "https://www.linkedin.com/in/example/"}'
```

The profile URL must be a valid LinkedIn profile in the format `https://www.linkedin.com/in/<profile-slug>/`.


## Architecture

```text
                        ┌─────────────────┐
                        │     Client      │
                        └────────┬────────┘
                                 │
                         POST /api/v1/profile
                                 │
                                 ▼
                        ┌─────────────────┐
                        │    Fastify API  │
                        └────────┬────────┘
                                 │
                                 ▼
                        ┌─────────────────┐
                        │ Profile Service │
                        └────────┬────────┘
                                 │
                                 ▼
                        ┌─────────────────┐
                        │ LinkedIn Client │
                        └────────┬────────┘
                                 │
                           Direct HTTP
                                 │
                                 ▼
                        ┌─────────────────┐
                        │    LinkedIn     │
                        └────────┬────────┘
                                 │
                                 ▼
                        ┌─────────────────┐
                        │ Parser / Mapper │
                        └────────┬────────┘
                                 │
                                 ▼
                           Structured JSON
```

The LinkedIn integration is kept separate from the API layer. This makes the upstream-specific code easier to test and replace as LinkedIn's internal APIs evolve.

## Tech Stack

* **TypeScript**
* **Node.js**
* **Fastify**
* **Zod**
* Native `fetch`
* **Vitest**
* **OpenAPI / Swagger**
* **Docker**

The project intentionally avoids a database and other infrastructure that isn't required for the current use case.

## Reverse Engineering

The LinkedIn integration is based on inspecting the HTTP traffic generated by LinkedIn and identifying the requests responsible for retrieving profile information.

The implementation then reproduces the relevant requests directly from the backend and maps the resulting data into a stable application-level schema.

This approach keeps the upstream protocol details isolated from the public API and makes changes to the LinkedIn integration easier to reason about.

### Technical Note: Pure HTTP Strategy and Limitations

#### What Was Attempted
A purely browserless HTTP reverse-engineering strategy was implemented to target LinkedIn's internal Server-Driven UI (SDUI) endpoints (`/flagship-web/rsc-action/actions/component`). The approach included:

1. **Native Request Replication:** Captured and reconstructed exact network payload requests for SDUI components containing profile sections (Skills, Education, Certifications, Languages).
2. **Header & Telemetry Matching:** Replicated required internal headers including `csrf-token`, `x-restli-protocol-version`, and client-tracing tokens (`parentSpanId`, `x-li-track`).
3. **TLS Impersonation:** Employed TLS/JA3 fingerprint spoofing to bypass network-level bot detection and avoid `500 Internal Server Error` rejections.

#### Limitations Encountered
While TLS impersonation and header reconstruction passed basic network checks, LinkedIn's backend enforces dynamic session state validation:

* **Dynamic Cookie & CSRF Rotation:** Ephemeral session cookies (`li_at`, `JSESSIONID`) and client tracking tokens rotate rapidly and require client-side JavaScript execution to stay synchronized with the backend.
* **Stateful Component Hydration:** Without an active browser runtime generating fresh client telemetry in real time, static HTTP calls fail to hydrate individual profile card components. The backend rejects requests that cannot prove they originated from a live JavaScript client.
* **Missing Section Data Below the Fold:** Profile sections like Skills, Education, Certifications, and Languages are lazy-loaded and not reliably present in the initial HTML document until the user scrolls or the section is activated.

#### Current Architecture
Because the core parsers (including `LinkedInSkillsParser`) are transport-agnostic and natively support parsing both rendered HTML and SDUI payloads, the extraction and normalization logic remains reusable. The implementation currently focuses on the top-card data (name, headline, location, profile image) and experience sections, which are more reliably available in the initial authenticated page response.

## API

### `POST /api/v1/profile`

Retrieves a LinkedIn profile.

#### Request

```json
{
  "url": "https://www.linkedin.com/in/example/"
}
```

#### `GET /health`

Returns the service health status.

```json
{
  "status": "ok"
}
```

### API Documentation

Interactive API documentation is available at:

```text
/docs
```

when running the application locally.

## Running Locally

### Requirements

* Node.js 20+
* npm
* A LinkedIn account/session for authenticated requests

Install dependencies:

```bash
npm install
```

Create the local environment file:

```bash
cp .env.example .env
```

Populate the required environment variables and start the development server:

```bash
npm run dev
```

### Other commands

```bash
npm test
npm run build
npm start
```

## Configuration

Authentication/session information is supplied through environment variables.

Sensitive values are never stored in the repository.

The repository contains an `.env.example` file showing the required configuration without exposing any actual credentials.

## Error Handling

The API handles common failure cases including:

* Invalid profile URLs
* Invalid request payloads
* Profile not found
* Authentication/session failures
* Upstream request failures
* Rate limiting
* Unexpected upstream responses
* Missing profile fields

Errors are returned as structured JSON with appropriate HTTP status codes.

## Security

The API validates incoming LinkedIn profile URLs and does not act as a generic HTTP proxy.

Credentials and session information are kept outside source control and should be provided through environment variables or deployment secrets.

The implementation does not attempt to bypass authentication, CAPTCHAs, access controls, rate limits, or other security mechanisms.

## Deployment

The API is designed to be deployed as a standalone Node.js service and exposed publicly over HTTPS, as required by the challenge.

Docker is included to keep the runtime environment reproducible.

The deployed endpoint will be added here once deployment is complete.

## Known Limitations

This project depends on undocumented LinkedIn endpoints, so the upstream behavior is not guaranteed to remain stable.

In particular:

* Endpoints and response formats may change.
* Authentication/session requirements may change.
* Some profile fields may not be available for every account.
* Profile visibility can affect the returned data.
* Rate limiting may affect requests.
* Profile images and other assets may not always be available.

The API therefore treats upstream data as optional where appropriate rather than assuming every profile contains every field.

## Legal / Terms

This implementation was created specifically for an engineering challenge that requests a reverse-engineered solution using direct LinkedIn HTTP endpoints.

LinkedIn's terms contain restrictions relating to automated access, scraping, and reverse engineering of its services. This project does not claim to be an officially supported LinkedIn integration.

It is intended for evaluation as part of the challenge and does not attempt to circumvent authentication, access controls, CAPTCHAs, or other security mechanisms.

Users deploying or extending the project are responsible for ensuring that their use complies with applicable laws, platform terms, and any required authorization.

## Project Status

**Active**

The API currently successfully retrieves:

* ✅ Name
* ✅ Headline
* ✅ Location
* ✅ Profile image
* ✅ Background image
* ✅ Experience (positions and employment history)

The following fields are present in the response schema but are empty due to LinkedIn's technical constraints documented in the Reverse Engineering section:

* ⚠️ About (About section not captured)
* ⚠️ Education (lazy-loaded below the fold)
* ⚠️ Skills (lazy-loaded below the fold, requires dynamic session hydration)
* ⚠️ Certifications (lazy-loaded below the fold)
* ⚠️ Languages (lazy-loaded below the fold)

These fields are unavailable because LinkedIn lazy-loads them after initial page render and requires active client-side JavaScript execution to hydrate the dynamic session state. The pure HTTP approach captures only the initial authenticated page response, which does not include these sections.
